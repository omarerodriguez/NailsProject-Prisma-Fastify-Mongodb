const {
  buildRecordAppointment,
} = require('../../builder/appointment/find-all-appointment-builder');

const {buildRecordTypesNails} = require('../../builder/nails/find-all-details-builder');
module.exports = {
  buildRecordAppointment,
  buildRecordTypesNails
};
